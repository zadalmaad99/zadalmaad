rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only signed-in admin(s) can read/write. Add more admin UIDs as needed.
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
